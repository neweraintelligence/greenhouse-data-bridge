services:
  - type: web
    name: greenhouse-data-bridge
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server.cjs
    envVars:
      - key: NODE_VERSION
        value: 18
